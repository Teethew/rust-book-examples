# rust-book-examples

Compilar o projeto (development mode):

```sh
cargo build
```

Executar o projeto:

```sh
cargo run
```

Exibir documentação das dependências utilizadas:

```sh
cargo doc --open
```